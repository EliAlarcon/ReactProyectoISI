export interface User {
    idUsuario: number;
    nombre: string;
    apellido: string;
    email: string;
    contrasena: string;
    tipo: string;
  }
  
  export interface Estudiante {
    // Define los campos específicos de Estudiante
  }
  
  export interface Profesor {
    // Define los campos específicos de Profesor
  }
  
  export interface Administrador {
    // Define los campos específicos de Administrador
  }